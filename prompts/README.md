# prompts

One entry per step of the build — the instruction that opened it, not a transcript.
The point is to show how the work was directed, not to log every keystroke.

Naming: `NN-<step>.md`. Each file holds the instruction, and a line on what came back
and what I changed by hand.
