# 01 — scaffold and spec

## Instruction

> Start from the disclosed generic scaffold (Hono on Cloudflare Workers, with
> hello-world wiring for x402, World ID and ENSv2 already in it). Do not build the
> project yet. First write `specs/requirements.md` by transcribing the qualification
> requirements of the prizes we are targeting, so that every later step can be checked
> against them.

## Outcome

Requirements transcribed. I wrote `specs/sketch.md` myself — the concept, the four
outcomes and the cut line are mine, and they came out of a conversation with a model
who described the actual problem.
