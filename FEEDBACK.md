# FEEDBACK — スポンサー SDK の統合レポート（雛形・当日は新 repo へコピーして埋める）

**なぜ先に持つか**: World の2賞と Uniswap の賞が**必須要件**としてこれを要求している（World＝「最初の成功までの時間・詰まった所・足りない機能・一番効く改善1つ」／Uniswap＝`FEEDBACK.md` ＋開発者フォーム）。日曜の朝に書くと薄くなるので、**触った直後にその場で1行ずつ足す**。数字（分・回数）は後から思い出せないので、計測しながら書く。

書き方: 褒めない。詰まった所を具体的に書く（エラーメッセージ・行き詰まったページ・誤解した語）。提案は1つに絞る。スポンサーは自社 docs の改善材料として読むので、**再現できる形**が価値。

---

## Cloudflare Workers + D1（自分の足元・参考）

- **Worker はリクエストごとに別インスタンス**なので、モジュールスコープの Map は本番で消える。ローカルの `wrangler dev` は1プロセスなので**通ってしまう**＝偽の合格を出す。デプロイして初めて露見した（09-25）。ハッカソンの最終日に見つけると痛い種類の罠。

## World ID（IDKit / World ID for Agents）

事前の「道具に慣れる」段階で一周済み（2026-09-22・本番アプリ＋実機の World App）。当日はここに追記する。

- **時間**: 事前検証は約90分（Portal でのアプリ作成から `ok:true` まで）。うち**80分は下記の User-Agent 403 の切り分け**で、実装そのものは10分程度
- **使った資格**: Proof of Human（device レベル）
- **なぜその資格か（最小十分の判断）**: 確かめたいのは「この操作をしたのが同じ人間か」の一点で、誰かを特定する必要はない。Orb もパスポートも過剰＝端末レベルで足りる。強い資格を積むほど良い設計にはしない
- **詰まった所**:
  1. **User-Agent が無いと nginx の 403 HTML**（下記・最大の時間損失）
  2. Developer Portal の「Verification」は掲載審査のフローで、action は「World ID Configuration」側。最初にここで迷った
  3. 既存アプリは後から staging に切り替えられない（新規作成時のみ選べる）
- **足りなかった機能・docs**: 管理 API（`/api/v1/graphql`）の公開ドキュメントが薄い。API キーで action の作成・更新はできるが、アプリの作成と `is_staging` の変更は不可＝**CLI だけで準備を完結できない**
- **一番効く改善1つ**: `/api/v2/verify` が UA 無しのリクエストにも **JSON でエラーを返すこと**（現状は HTML の 403 ＝ クライアントの `res.json()` が構文エラーで落ち、本当の原因に辿り着けない）
- **失敗経路の実装**（拒否・期限切れ・取消・資格なし）で気づいたこと: `___`
- **docs に無かった挙動（09-22 実測）**: `/api/v2/verify` は **User-Agent が無いリクエストを nginx の 403 HTML で弾く**。Cloudflare Workers の `fetch` は既定で UA を送らないので、正しい app_id と action でも全て 403 になる。本文が JSON でないため `res.json()` が `Unexpected token '<'` で落ち、原因が隠れる（未知の app_id も同じ 403 HTML なので、誤って「app_id が違う」と判断しかけた）。UA を1つ付けるだけで解決。

## ENS（ENSv2 Sepolia）

事前の「道具に慣れる」段階（2026-09-21〜22）。**読む側は動いた／登録は初見で詰まる箇所が多い**。当日に追記する。

- **結果**: 読み取りは動いた。**テスト名の登録は完了しなかった**（09-22・1時間以上を費やして打ち切り）。最終状態＝アプリ上は支払いまで進み「Enabling sessions…」で停止、**ウォレットには署名要求が届かず、アドレスからのトランザクションは1件も出ていない**（nonce 0・残高も未変化＝チェーン上では何も起きていない）
- **時間**: 読み取り（`scripts/ens.ts`）は約40分。登録は1時間以上で未達
- **使った機能**: 名前の解決・テキストレコードの読み取り・逆引き（段1）。サブネーム発行は当日
- **詰まった所（登録までの順に）**:
  1. **viem の `getEnsAddress` / `getEnsText` が ENSv2 の Universal Resolver で `null` を返す**。docs どおり `resolve(bytes,bytes)` を直接呼べば引ける。helper が v2 に追随していないことは「ENSv2 readiness」のページからは読み取れなかった（`scripts/ens.ts`）
  2. **ウォレットの Testnet Mode が必要**。Phantom は既定で testnet を隠すので、app.ens.dev が「You are not in Testnet mode」で止まる。設定の場所は docs でなくウォレット側にあり、初見では辿れない
  3. **支払いトークンが2種類あって紛らわしい**。ETHGlobal の faucet が配るのは Circle のテスト USDC（`0x1c7D…`）だが、ENSv2 が受け取るのは専用の MockUSDC（`0x768f…`）。**残高があるのに払えない**状態になる。MockUSDC は誰でも無料で `mint(to, amount)` を呼べる（検証済み）が、**登録画面からその導線が見えなかった**
  4. ガス用の Sepolia ETH と、ETHGlobal の faucet の関係が分かりにくい（同じ「Ethereum Sepolia」でも ETH を配る面と USDC を配る面が別カード）
  5. **「Enabling sessions…」で長く止まる**。ウォレットに承認要求が届いていない時と、単に待っている時の区別が画面から付かない
  6. **Sepolia の ENSv2 は状態が定期的にリセットされる**とアプリ内に告知がある（直近 2026-09-15）。練習で作った名前が消える前提で進める必要があり、これは最初に知りたかった
  7. **最終的に登録が完了しなかった**。画面は進み続けるのにウォレットへ署名要求が来ず、切り分ける手段（どの段階で何を待っているか）が画面にもコンソールにも無い
- **足りなかった機能・docs**: ①サブネーム発行の TypeScript 例が無い（Solidity のチュートリアルのみ）②Sepolia のテスト名を取るまでの前提（Testnet Mode・MockUSDC・ガス）が1ページにまとまっていない
- **一番効く改善1つ**: **登録画面に「テスト用トークンを mint する」ボタンを置くこと**。誰でも呼べる `mint` が既にあるのに導線が無く、ここで一番時間を失った（ハッカソン当日は全員がこの1点で詰まる）

## x402（使った場合）

- **時間**: 開始 → 402 が返った → 支払いが通った（= `__` 分）
- 09-16 に実測済み＝`@x402/hono` の middleware 1本と `@x402/fetch` で一周した。未入金だと facilitator が `invalid_exact_evm_insufficient_balance` を返す（エラー本文はヘッダの base64 の中で、本文は `{}` のみ＝**最初ここで詰まる**）
- **詰まった所**: `___`
- **一番効く改善1つ**: `___`

## Uniswap（使った場合）

- 提出前に開発者フィードバックフォーム（https://developers.uniswap.org/hackathon-feedback）に本ファイルの URL を貼る。README に統合箇所の行番号を書く（審査で確認される）
- **時間 / 詰まった所 / 改善1つ**: `___`

---

## 当日の運用

- 触り始めに時刻をメモ（`date` でよい）。成功した瞬間にもメモ。
- 詰まったら**その場で1行**。解決してから書くと「大したことなかった」と丸めてしまう。
- 提出前にこのファイルを新 repo の README からリンクし、World の submission 欄にも要点3行を貼る。
