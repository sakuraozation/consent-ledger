# CLAUDE.md

ETHGlobal Tokyo 2026 の提出物。36時間で作る。**この repo は公開**＝顧客名・他人の実名・秘密を書かない。

## いま作っているもの（21:00 以降に埋める）

- **誰の何を解くか**: <1文>
- **デモで見せる1画面**: <3状態を1行ずつ>
- **狙うスポンサー賞（最大3）**: <例 Intercepta / World IDKit / World for Agents>
- **経路（3ステップ）**: <入力 → SDK 呼び出し → 出力>
- **作らないと決めた物**: <列挙>

詳細＝`specs/sketch.md`。この5行と `specs/` が食い違ったら `specs/` が正しい。

## 進め方

- **経路を端から端まで通すのが先**。機能を足すのは通った後。土曜 18:00 に通っていなければ、**足さずに経路を短くする**（入力を固定値にする・画面を1つ減らす）
- **1工程ごとに commit**（単一の巨大コミットは失格対象）。メッセージは「何を」1行＋「なぜ」
- **デモの台本に出てこない機能は作らない**
- 迷ったら削る

## AI の使い方（提出要件）

- 指示は `prompts/` に残す。仕様と計画は `specs/` に置く
- **`AI-USE.md` を更新しながら進める**＝どのファイルを AI が書き、どこを人が設計・修正したか
- AI に丸投げした物だけでは賞の対象外。**判断（経路・削る物・権限や予算のロジック・台本）は人が持つ**

**AI を使ってはいけない所（規約の明文）**
- **デモ動画の合成音声**＝失格。「音楽＋説明テロップだけ」も不可＝**自分の声で話す**
- **外部 API の応答をモックやハードコードで代用しない**（Intercepta の要件。実際に呼ぶ）
- **FEEDBACK の数値を生成させない**＝実測（最初の成功までの時間・詰まった所）。作ると評価軸で落ちる

## スポンサー SDK

- **公式 docs の URL を渡して読ませる**。学習データが薄いので推測で書かせない
- 統合は**浅く3社より深く1社**。賞は最大3つまで申請できるが、経路に自然に乗る分だけ
- 触った直後に `FEEDBACK.md` へ1行（時間・詰まった所・足りない物・一番効く改善1つ）。**日曜の朝に書くと薄くなる**

## 技術

- Bun / TypeScript / Hono on Cloudflare Workers（`bun run dev` → localhost:8787）
- 事前に用意した汎用の雛形（開示済み・README 参照）＝x402 の支払い側、World ID のサーバ検証と失敗経路、ENSv2 の読み取り
- 秘密は `.dev.vars`（git 管理外）。`wrangler.toml` の `[vars]` に秘密を書かない

## 提出（日曜 09:00 締切・08:45 には座っている）

- [ ] 公開 repo。README が**統合箇所のファイルと行**を指す
- [ ] デモ動画 2〜4分・720p 以上・**自分の声**（合成音声は失格）・早回し不可・スマホ撮影不可
- [ ] **成功する経路と、失敗する経路の両方**を動画で見せる
- [ ] `specs/` `prompts/` `AI-USE.md` `FEEDBACK.md` が入っている
- [ ] 賞ごとの要件を `specs/requirements.md` で1つずつ潰す
