{
  "name": "consent-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "hono": "^4.12.32",
    "jose": "^6.2.12",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.2.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.115.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "pay": "bun run scripts/pay.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bunx biome ci . && bunx tsc --noEmit && bun test"
  }
}
